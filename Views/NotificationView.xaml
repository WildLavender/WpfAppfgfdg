<UserControl x:Class="CollegeGradeSystem.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="🔔 Уведомления" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
            <Button Content="🔄 Обновить" Command="{Binding LoadCommand}" Margin="20,0,0,0" Style="{StaticResource PrimaryButton}" Height="35" Width="120"/>
            <Button Content="✓ Все прочитано" Command="{Binding MarkAllAsReadCommand}" Margin="10,0,0,0" Style="{StaticResource SuccessButton}" Height="35" Width="140" Visibility="{Binding HasUnread, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="📬 Непрочитанные" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="{DynamicResource PrimaryBrush}" Visibility="{Binding UnreadNotifications.Count, Converter={StaticResource BoolToVis}}"/>

                <ItemsControl ItemsSource="{Binding UnreadNotifications}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15" Margin="0,5" CornerRadius="5" BorderBrush="#FFC107" BorderThickness="2" Background="#FFF3CD" Cursor="Hand">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}"/>
                                </Border.InputBindings>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="#856404"/>
                                        <TextBlock Text=" • " Foreground="#856404" Margin="5,0"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM HH:mm}'}" FontSize="12" Foreground="#856404"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#856404"/>
                                    <Button Content="✓ Прочитано" Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}" HorizontalAlignment="Right" Style="{StaticResource SecondaryButton}" Height="28" Width="100"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,20" Visibility="{Binding UnreadNotifications.Count, Converter={StaticResource BoolToVis}}"/>

                <TextBlock Text="✅ Прочитанные" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="{DynamicResource TextMutedBrush}"/>

                <ItemsControl ItemsSource="{Binding ReadNotifications}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15" Margin="0,5" CornerRadius="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource WindowBackgroundBrush}" Opacity="0.8">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBlock Text=" • " Foreground="{DynamicResource TextMutedBrush}" Margin="5,0"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM HH:mm}'}" FontSize="12" Foreground="{DynamicResource TextMutedBrush}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="📭 Уведомлений нет" TextAlignment="Center" FontSize="16" Foreground="{DynamicResource TextMutedBrush}" Margin="0,40" Visibility="{Binding AllNotifications.Count, Converter={StaticResource BoolToVis}, ConverterParameter='Inverse'}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>