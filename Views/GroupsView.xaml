<UserControl x:Class="CollegeGradeSystem.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопка обновления -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="📚 Справочник групп" 
                       Style="{StaticResource TitleTextBlock}"/>

            <Button Grid.Column="1" Content="🔄 Обновить" 
                    Command="{Binding RefreshCommand}"
                    Padding="20,10"
                    Style="{StaticResource PrimaryButton}"/>
        </Grid>

        <!-- Таблица групп -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Groups}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id_group}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding name}" Width="150"/>
                <DataGridTextColumn Header="Курс" Binding="{Binding course}" Width="80"/>
                <DataGridTextColumn Header="Куратор ID" Binding="{Binding curator_id}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>