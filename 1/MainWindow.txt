<Window x:Class="CollegeGradeSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        d:DesignHeight="945" d:DesignWidth="1200"
        Title="ИС Успеваемости - Управление студентами" 
        Height="945" Width="1200"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <!-- Стили для ComboBoxItem в этом окне -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource ComboBoxPopupBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboBoxHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с навигацией и темой -->
        <Border Grid.Row="0" Background="{DynamicResource WindowBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Навигация -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="📚 Студенты" Click="btnStudents_Click"
                            Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="👥 Группы" Click="btnGroups_Click"
                            Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="📊 Отчёты" Click="btnReports_Click"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>

                <!-- Переключатель темы -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="🌙 Тема:" VerticalAlignment="Center" Margin="0,0,10,0"
                               Foreground="{DynamicResource TextBrush}"/>
                    <ComboBox x:Name="cmbTheme" Width="120" Height="35"
                              SelectionChanged="cmbTheme_SelectionChanged"
                              SelectedIndex="0">
                        <ComboBoxItem Content="☀️ Светлая" Tag="LightTheme"/>
                        <ComboBoxItem Content="🌙 Тёмная" Tag="DarkTheme"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Контент (UserControl) -->
        <ContentControl Grid.Row="1" x:Name="MainContent"/>
    </Grid>
</Window>