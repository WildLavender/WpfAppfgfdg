<UserControl x:Class="CollegeGradeSystem.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель поиска и фильтрации -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                Background="{DynamicResource WindowBackgroundBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="35" Margin="0,0,10,0"
                         VerticalContentAlignment="Center"/>

                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding Groups}"
                          DisplayMemberPath="name"
                          SelectedValuePath="id_group"
                          SelectedValue="{Binding SelectedGroupId}"
                          Height="35" Margin="0,0,10,0"/>

                <Button Grid.Column="2" Content="🔍 Найти" 
                        Command="{Binding SearchCommand}"
                        Height="35" Width="100" Margin="0,0,10,0"
                        Style="{StaticResource PrimaryButton}"/>

                <Button Grid.Column="3" Content="✕ Сброс" 
                        Command="{Binding ClearFilterCommand}"
                        Height="35" Width="100" Margin="0,0,10,0"
                        Style="{StaticResource SecondaryButton}"/>

                <Button Grid.Column="4" Content="📊 Экспорт" 
                        Command="{Binding ExportCommand}"
                        Height="35" Width="100"
                        Style="{StaticResource SuccessButton}"/>
            </Grid>
        </Border>

        <!-- Таблица студентов -->
        <DataGrid Grid.Row="1" Grid.Column="0" Margin="0,0,15,0"
                  ItemsSource="{Binding Students}"
                  SelectedItem="{Binding SelectedStudent, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id_student}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding full_name}" Width="200"/>
                <DataGridTextColumn Header="Дата рождения" 
                                    Binding="{Binding birth_date, StringFormat={}{0:dd.MM.yyyy}}" 
                                    Width="120"/>
                <DataGridTextColumn Header="Группа" Binding="{Binding group_name}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма добавления/редактирования -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="{DynamicResource WindowBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="5"
                Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Данные студента" 
                           Style="{StaticResource TitleTextBlock}"
                           Margin="0,0,0,15"/>

                <TextBlock Grid.Row="1" Text="ID:" Margin="0,0,0,5"/>
                <TextBox Grid.Row="2" 
                         Text="{Binding EditStudent.id_student, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="False"
                         Height="35" Margin="0,0,0,15"/>

                <TextBlock Grid.Row="3" Text="ФИО:" Margin="0,0,0,5"/>
                <TextBox Grid.Row="4" 
                         Text="{Binding EditStudent.full_name, UpdateSourceTrigger=PropertyChanged}"
                         Height="35" Margin="0,0,0,15"/>

                <TextBlock Grid.Row="5" Text="Дата рождения:" Margin="0,0,0,5"/>
                <DatePicker Grid.Row="6" 
                            SelectedDate="{Binding EditStudent.birth_date, UpdateSourceTrigger=PropertyChanged}"
                            Height="35" 
                            MinWidth="150"
                            Margin="0,0,0,15"/>

                <TextBlock Grid.Row="7" Text="Группа:" Margin="0,0,0,5"/>
                <ComboBox Grid.Row="8"
                          ItemsSource="{Binding Groups}"
                          DisplayMemberPath="name"
                          SelectedValuePath="id_group"
                          SelectedValue="{Binding EditStudent.id_group, UpdateSourceTrigger=PropertyChanged}"
                          Height="35" 
                          MinWidth="150"
                          Margin="0,0,0,15"/>

                <TextBlock Grid.Row="9" Text="Email:" Margin="0,0,0,5"/>
                <TextBox Grid.Row="10" 
                         Text="{Binding EditStudent.email, UpdateSourceTrigger=PropertyChanged}"
                         Height="35" 
                         MinWidth="150"
                         Margin="0,0,0,15"/>

                <!-- Кнопки управления -->
                <StackPanel Grid.Row="11" Margin="0,20,0,0">
                    <Button Content="➕ Добавить нового" 
                            Command="{Binding AddCommand}"
                            Height="40" Margin="0,0,0,10"
                            Style="{StaticResource PrimaryButton}"/>

                    <Button Content="💾 Сохранить" 
                            Command="{Binding SaveCommand}"
                            Height="40" Margin="0,0,0,10"
                            Style="{StaticResource SuccessButton}"/>

                    <Button Content="🗑 Удалить" 
                            Command="{Binding DeleteCommand}"
                            Height="40" Margin="0,0,0,10"
                            Style="{StaticResource DangerButton}"/>

                    <Button Content="🔄 Обновить список" 
                            Command="{Binding RefreshCommand}"
                            Height="40"
                            Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>